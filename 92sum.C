#include<stdio.h>
#include<conio.h>
void main()
{
int i,n,sum=0,a[10];
printf("\n enter the number:");
scanf("%d",&n);
scanf("%d%d"&a,&b);
for(i=0;i<n;i++)
{
scanf("%d",&a[i]);
sum=sum+a[i];
}
printf("\n  the sum is:",sum);
getch();
}
